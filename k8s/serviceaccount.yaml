# Identity the secrets-operator presents to Infisical for this app's
# InfisicalSecret CR (kubernetesAuth). The ai-jakegaylor-com-operator machine identity
# on the server is restricted to exactly this SA + namespace.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: ai-jakegaylor-com-infisical
  namespace: ai-jakegaylor-com
